rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 2-Sektor Paxtachi - Appeals, Organizations, Settings collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
